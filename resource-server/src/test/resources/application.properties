# Base de datos en memoria
spring.datasource.url=jdbc:h2:mem:resourcedb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# --- LA CLAVE ESTÁ AQUÍ ---
# No ponemos la propiedad issuer-uri aquí para que no intente conectar automáticamente.
# En su lugar, definiremos un Mock en el código del test o usaremos una propiedad dummy que no valide conexión.